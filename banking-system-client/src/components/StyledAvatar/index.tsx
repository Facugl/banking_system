import { type AvatarProps } from '@mui/material';
import { StyledAvatarStyled } from './styles';

const StyledAvatar = (props: AvatarProps) => <StyledAvatarStyled {...props} />;

export default StyledAvatar;
