import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

export const StatsBoxStyled = styled(Box)(({ theme }) => ({
  textAlign: 'center',
  padding: theme.spacing(2),
}));
